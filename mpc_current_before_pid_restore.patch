diff --git a/src/mpc_controller/src/config/mpc_params.yaml b/src/mpc_controller/src/config/mpc_params.yaml
index c94175b..81feb23 100644
--- a/src/mpc_controller/src/config/mpc_params.yaml
+++ b/src/mpc_controller/src/config/mpc_params.yaml
@@ -8,18 +8,18 @@ control_frequency: 20.0
 
 # MPC solver
 mpc:
-  horizon: 10
+  horizon: 30
   dt: 0.1
   max_iterations: 25
   wheelbase: 2.7
 
 # Cost weights
 cost:
-  path_error: 10.0
-  heading_error: 2.0
-  speed_error: 0.25
-  control: 2.0
-  control_rate: 20 
+  path_error: 5.0
+  heading_error: 3.0
+  speed_error: 5.0
+  control: 0.1
+  control_rate: 5.0 
   obstacle: 10.0
   terminal: 5.0
 
@@ -34,10 +34,10 @@ solver:
 constraints:
   steering:
     max_angle: 0.6109   # ~35 deg [rad]
-    max_rate: 0.4       # [rad/s]
+    max_rate: 0.8       # [rad/s]
   acceleration:
     max_forward: 2.0    # [m/s^2]
-    max_reverse: -5.0   # [m/s^2]
+    max_reverse: -4.0   # [m/s^2]
   velocity:
     min: 0.0            # [m/s]
     max: 30.0           # [m/s]
@@ -47,21 +47,21 @@ constraints:
 
 # Reference / speed planning
 planning:
-  ref_window: 120            # waypoints in reference window
-  target_vel: 10.66667      # 60 km/h in m/s
-  curve_vel_sharp: 10.333    # 30 km/h
-  curve_vel_mid:   13.722    # 35 km/h
-  curve_vel_mild:  14.5     # 45 km/h
-  curve_th_sharp:  0.01
-  curve_th_mid:    0.004
-  curve_th_mild:   0.001
+  ref_window: 700            # waypoints in reference window
+  target_vel: 10.0     # 60 km/h in m/s(10.66667)
+  curve_vel_sharp: 4.0    # 30 km/h(10.333)
+  curve_vel_mid:   6.0    # 35 km/h(13.722)
+  curve_vel_mild:  8.0     # 45 km/h(14.5)
+  curve_th_sharp:  0.080
+  curve_th_mid:    0.035
+  curve_th_mild:   0.015
 
 # Longitudinal PID (m/s error → normalized [0,1] accel/brake)
 pid:
-  kp: 0.05
-  ki: 0.01
+  kp: 0.1
+  ki: 0.003
   kd: 0.001
 
 # CSV file paths (workspace 기준 상대경로)
-waypoint_file: "/root/aim_ws/src/main/config/track_log_recorded_final.csv"
-ref_file:      "/root/aim_ws/src/main/config/ref.txt"
+waypoint_file: "/home/autonav/mpc_ws/src/mpc_controller/src/config/path.txt"
+ref_file:      "/home/autonav/mpc_ws/src/mpc_controller/src/config/ref.txt"
diff --git a/src/mpc_controller/src/global/parameter_loader.cpp b/src/mpc_controller/src/global/parameter_loader.cpp
index b3215ef..52868d3 100644
--- a/src/mpc_controller/src/global/parameter_loader.cpp
+++ b/src/mpc_controller/src/global/parameter_loader.cpp
@@ -80,10 +80,11 @@ void computeWaypointCurvatures()
     if (waypoints.size() < 3) return;
 
     auto curvAt = [&](size_t i)->double {
-        if (i == 0 || i + 1 >= waypoints.size()) return 0.0;
-        const auto& p0 = waypoints[i-1];
+        const size_t gap = 25;
+        if (i < gap || i + gap >= waypoints.size()) return 0.0;
+        const auto& p0 = waypoints[i-gap];
         const auto& p1 = waypoints[i];
-        const auto& p2 = waypoints[i+1];
+        const auto& p2 = waypoints[i+gap];
         double a = std::hypot(p1.x-p0.x, p1.y-p0.y);
         double b = std::hypot(p2.x-p1.x, p2.y-p1.y);
         double c = std::hypot(p2.x-p0.x, p2.y-p0.y);
diff --git a/src/mpc_controller/src/node/mpc_node.cpp b/src/mpc_controller/src/node/mpc_node.cpp
index 2ff8a8c..e354e0c 100644
--- a/src/mpc_controller/src/node/mpc_node.cpp
+++ b/src/mpc_controller/src/node/mpc_node.cpp
@@ -56,7 +56,10 @@ void CBEgoState(const morai_msgs::EgoVehicleStatus::ConstPtr& msg)
     if (!msg) return;
     // PlanningControl 과 동일: 속도만 사용 (yaw 는 IMU 가 권위)
     std::lock_guard<std::mutex> lk(ego_mutex);
-    ego.vx = msg->velocity.x;
+    //ego.vx = msg->velocity.x;
+    double v_forward = msg->velocity.x * std::cos(ego.yaw)
+                 + msg->velocity.y * std::sin(ego.yaw);
+    ego.vx = std::max(0.0, v_forward);
 }
 
 void CBCostmap(const nav_msgs::OccupancyGrid::ConstPtr& msg)
@@ -159,6 +162,42 @@ void buildReferenceFromWaypoints()
         reference_path.yaw_ref.push_back(yaw_ref);
         reference_path.v_ref.push_back(velocityFromCurvature(waypoints[i].curvature));
     }
+    // v_ref smoothing: 계단식 속도 변화를 완화
+    if (reference_path.v_ref.size() >= 3) {
+        std::vector<double> smoothed = reference_path.v_ref;
+
+        int smooth_radius = 30;  // waypoint 간격 0.1m면 앞뒤 약 0.5m
+        for (size_t i = 0; i < reference_path.v_ref.size(); ++i) {
+            size_t start = (i > static_cast<size_t>(smooth_radius)) ? i - smooth_radius : 0;
+            size_t end = std::min(reference_path.v_ref.size() - 1, i + static_cast<size_t>(smooth_radius));
+
+            double sum = 0.0;
+            int count = 0;
+            for (size_t j = start; j <= end; ++j) {
+                sum += reference_path.v_ref[j];
+                ++count;
+            }
+            smoothed[i] = sum / count;
+        }
+
+        reference_path.v_ref = smoothed;
+    }
+    // Backward speed propagation for MPC longitudinal preview.
+    // If a low-speed curve exists ahead, limit preceding points so the vehicle can decelerate smoothly.
+    if (reference_path.v_ref.size() >= 2) {
+        const double decel = 0.05;  // [m/s^2], smaller = earlier/slower approach
+
+        for (int i = static_cast<int>(reference_path.v_ref.size()) - 2; i >= 0; --i) {
+            double dx = reference_path.x_ref[i + 1] - reference_path.x_ref[i];
+            double dy = reference_path.y_ref[i + 1] - reference_path.y_ref[i];
+            double ds = std::hypot(dx, dy);
+
+            double v_next = reference_path.v_ref[i + 1];
+            double v_allowed = std::sqrt(v_next * v_next + 2.0 * decel * ds);
+
+            reference_path.v_ref[i] = std::min(reference_path.v_ref[i], v_allowed);
+        }
+    }
     new_reference_path_received = true;
 }
 
@@ -204,7 +243,7 @@ void publishCtrlCmd(double steering_rad,
 {
     morai_msgs::CtrlCmd cmd;
     cmd.longlCmdType = 1;                              // accel/brake mode
-    cmd.front_steer     = steering_rad;                   // [rad]
+    cmd.front_steer     = -steering_rad;                   // [rad] 부호 반전
     cmd.accel        = clip(accel_norm, 0.0, 1.0);
     cmd.brake        = clip(brake_norm, 0.0, 1.0);
     cmd_pub.publish(cmd);
@@ -264,11 +303,74 @@ void controlLoop(const ros::TimerEvent&)
 }
 
     // 6) 종방향: MPC 가 본 reference 의 첫 번째 v_ref 를 target 으로 사용
-    double v_target = reference_path.v_ref.empty()
-                          ? mpc_params.target_vel
-                          : reference_path.v_ref.front();
-    double accel_norm, brake_norm;
-    velocityPID(v_target, ego_snap.vx, accel_norm, brake_norm);
+    //double v_target = reference_path.v_ref.empty()
+    //                      ? mpc_params.target_vel
+    //                      : reference_path.v_ref.front();
+    double v_target = mpc_params.target_vel;
+    double debug_min_v = v_target;
+    double debug_dist_to_min_v = 0.0;
+    double debug_v_allowed = v_target;
+
+    if (!reference_path.v_ref.empty()) {
+        // 기본은 현재 위치의 reference 속도
+        v_target = reference_path.v_ref.front();
+
+        // 앞쪽 preview 거리 설정
+        const double preview_distance = 70.0;  // [m], 도심 90도 코너면 15~25m 후보
+        const double comfortable_decel = 0.7;  // [m/s^2]
+
+        double accum_dist = 0.0;
+        double min_v = reference_path.v_ref.front();
+        double dist_to_min_v = 0.0;
+
+        for (size_t i = 1; i < reference_path.v_ref.size(); ++i) {
+            double dx = reference_path.x_ref[i] - reference_path.x_ref[i - 1];
+            double dy = reference_path.y_ref[i] - reference_path.y_ref[i - 1];
+            accum_dist += std::hypot(dx, dy);
+
+            if (accum_dist > preview_distance) {
+                break;
+            }
+
+            if (reference_path.v_ref[i] < min_v) {
+                min_v = reference_path.v_ref[i];
+                dist_to_min_v = accum_dist;
+            }
+        }
+
+        // 코너 속도 min_v까지 남은 거리 dist_to_min_v를 고려한 허용 현재속도
+        double v_allowed = std::sqrt(min_v * min_v + 2.0 * comfortable_decel * dist_to_min_v);
+
+        // 너무 높은 목표를 막고, 코너 접근 시 자연스럽게 낮춤
+        v_target = std::min(v_target, v_allowed);
+        v_target = std::min(v_target, mpc_params.target_vel);
+
+        // 안전 하한. 너무 저속 고정 방지용, 필요 없으면 생략 가능
+        v_target = std::max(v_target, 2.0);
+        debug_min_v = min_v;
+        debug_dist_to_min_v = dist_to_min_v;
+        debug_v_allowed = v_allowed;
+    }
+    //double accel_norm, brake_norm;
+    //velocityPID(v_target, ego_snap.vx, accel_norm, brake_norm);
+    double accel_norm = 0.0;
+    double brake_norm = 0.0;
+
+    static double a_cmd_filtered = 0.0;
+    const double accel_filter_alpha = 0.15;  // smaller = smoother actuator command
+    double a_cmd_raw = res.control.accel;
+    double a_cmd = accel_filter_alpha * a_cmd_raw
+                 + (1.0 - accel_filter_alpha) * a_cmd_filtered;
+    a_cmd_filtered = a_cmd;
+
+    const double accel_pedal_scale = 4.0;
+    const double brake_pedal_scale = 2.0;
+
+    if (a_cmd >= 0.0) {
+        accel_norm = clip(a_cmd / accel_pedal_scale, 0.0, 1.0);
+    } else {
+        brake_norm = clip(-a_cmd / brake_pedal_scale, 0.0, 1.0);
+    }
 
     // 7) 발행 (steering 은 MPC 결과, accel/brake 는 PID 결과)
     double steer = clip(res.control.delta,
@@ -277,7 +379,8 @@ void controlLoop(const ros::TimerEvent&)
     publishCtrlCmd(steer, accel_norm, brake_norm);
 
     ROS_INFO_THROTTLE(1.0,
-        "[MPC] pos=(%.2f,%.2f) yaw=%.2f vx=%.2f | tgt=%.2f m/s | steer=%.3f rad | accel=%.2f brake=%.2f | cost=%.2f",
+        "[MPC] pos=(%.2f,%.2f) yaw=%.2f vx=%.2f | tgt=%.2f min_v=%.2f dist=%.1f allow=%.2f | steer=%.3f rad | a_raw=%.2f a_filt=%.2f accel=%.2f brake=%.2f | cost=%.2f",
         ego_snap.x, ego_snap.y, ego_snap.yaw, ego_snap.vx,
-        v_target, steer, accel_norm, brake_norm, res.cost);
+        v_target, debug_min_v, debug_dist_to_min_v, debug_v_allowed,
+        steer, a_cmd_raw, a_cmd, accel_norm, brake_norm, res.cost);
 }
